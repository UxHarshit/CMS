---
import Layout from '../layouts/Layout.astro';
import Landing from '../components/Landing.tsx';
import '../global.css';
const token = Astro.cookies.get("token");
const isLogged = token !== undefined;
---

<Layout>
	<Landing client:load isLoggedIn={isLogged} />
</Layout>
